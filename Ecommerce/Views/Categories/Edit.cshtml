@model Ecommerce.Models.Category

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Edit Category";
    ViewData["PageTitle"] = "Edit Category";
    ViewData["BreadcrumbParent"] = "Categories";
    ViewData["BreadcrumbParentUrl"] = Url.Action("Index", "Categories");
    ViewData["BreadcrumbCurrent"] = "Edit";
}

<div class="admin-card admin-form-card">
    <div class="admin-card-header">
        <span class="card-title">✏️ Edit Category</span>
        <a asp-action="Index" class="btn-admin-secondary">← Back to Categories</a>
    </div>
    <div class="admin-card-body">
        <form asp-action="Edit" class="admin-form">
            <div asp-validation-summary="ModelOnly" class="validation-summary"></div>
            <input type="hidden" asp-for="Id" />

            <div class="form-field">
                <label asp-for="Name">Category Name</label>
                <input asp-for="Name" />
                <span asp-validation-for="Name" class="field-error"></span>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn-admin-primary">✓ Save Changes</button>
                <a asp-action="Index" class="btn-admin-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
